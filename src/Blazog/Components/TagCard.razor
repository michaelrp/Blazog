<div class="col mb-4">
    <div class="card text-white bg-info h-100">
        <div class="card-body">
            <h5 class="card-title"><NavLink class="text-reset" href="@($"/tag/{Info.Title}")" title="@(Info.Title)">@Info.Title</NavLink></h5>
            <p class="card-text">@Info.About</p>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public TagInfo Info { get; set; }
}